# nixfiles

my nix-darwin + home-manager config. multi-host — otter (mac) now, a nixos box later.

```sh
just switch   # rebuild this host
just check    # fmt + build gate
```

theme lives in `theme.nix`; one file per program under `home/modules/`.
