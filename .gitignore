# nix build artifacts
result
result-*

# direnv
.direnv/

# macOS cruft
.DS_Store

# raw secret material, never tracked. sops *ciphertext* is fine to commit; these
# plaintext forms are not. The gitleaks pre-commit hook is the real backstop.
keys.txt
**/age/keys.txt
AGE-SECRET-KEY-*
.netrc
netrc

# python bytecode
__pycache__/
*.pyc

# pyghidra-mcp creates a ghidra project dir in cwd when run from the repo root
pyghidra_mcp_projects/
